#include <iostream>
#include <vector>
using namespace std;
class Solution {
   public:
    int divisibleGame(vector<int>& nums) {
        vector<vector<int>> pos();
        vector<int> prefix(nums);
        // int maxVal = *max_element(nums.begin(), nums.end());
        // vector<int> k_pos(maxVal + 1);
        // for()
    }
};
// ©leetcode